#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print some letter of alphabet
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	{
		if (c != e && low != q)
		{
			putchar(c);
		}
	}

	putchar('\n');

	return (0);
